#----CORE API AND WEB FRAMEWORK
fastapi
uvicorn[standard]
python-dotenv
requests         # for HTTP library for internal or external API calls
numpy            # for numerical operations and array manipulation
pandas           # for data manipulation and analysis  


# ----- GEMINI EMBEDDINGS --------
google-generativeai         # Gemini API -- embeddings (text-embedding
langchain-google-genai    # LangChain integration for Gemini models
sentence-transformers    # local fallback embedding model if the Gemini API is unavailable 


# -----VECTOR DB & RETRIEVAL ------
qdrant-client          # Vector DB for semantic search


# ----- LANGCHAIN AND AGENTIC ORCHESTRATION ----
langchain     # Core LLM orchestration
langgraph     # State-machine logic
langchain-groq    
pydantic>=2.0.0             # Data validation and setting


# -------- CORE API AND WEB FRAMEWORK ----------
fastapi      # High-performance web framework
uvicorn[standard]      # ASGI server to run the FastAPI
python-dotenv    # For loading configuration ..
requests        # HTTP library for internal/external interaction
streamlit      # Frontend chat interface
numpy           # Numerical processing


# -----DATA INGESTION AND DOCUMENT PARSING
unstructured>=0.22.0       # Advanced PDF parsing and text extraction    
python-pptx        # For .ppt and .pptx file extraction 
python-docx        # For .docx file extraction
pypdf             # For PDF document parsing and text extraction
beautifulsoup4      # For HTML content parsing   
pdfplumber          # fallback PDF parser for scanned/complex layouts
langchain-text-splitters    # For splitting documents for chunking


# ------ OBSERVABILITY & DISTRIBUTED TRACING -------
logfire[fastapi,requests]       # Pydantic logfire - traces the ingestion pipeline
loguru                          # Enhanced logging for python
langsmith                       # Specialized tracking for LangChain/LangGraph


# RERANKING IN RAG
flashrank

# ADD UVICORN FOR BACKEND PROCESS (FASTAPI)
uvicorn


# ------ GUARDRAILS ------
nemoguardrails                    # NVIDIA NeMo Guardrails for input/output
langchain-nvidia-ai-endpoints     # Langchain integration with nvidia
langchain-google-vertexai         # required shim for NeMo Guardrails


# ----- LLM GATEWAY -------
portkey-ai              # Portkey Gateway for unified LLM routing

# ---- EVALUATION (EVALS) -----------
ragas                          # RAG-specific evaluation metircs (Faithfulness, Relevancy, Recall)
deepeval                       # Pytest-compatible test runner wrapping RAGAS & safety metrics
langfuse                       # Live production monitoring, tracing & user feedback collection
openai                         # AsyncOpenAI client used directly by the RAGAS judge LLM


